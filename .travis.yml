language: java
sudo: false

cache:
  directories:
  - "$HOME/.m2"

jobs:
  include:
  - stage: test
    jdk: openjdk11
    script: mvn verify -Dmaven.test.redirectTestOutputToFile=true -Dsurefire-forkcount=true -Dzookeeper.forceSync=no

branches:
  only:
  - master
